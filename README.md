# da-landing

Landing page for the DA app beta.

## Run locally

```bash
python3 -m http.server 8080
```

Then open [http://localhost:8080](http://localhost:8080).

To test on mobile from the same Wi-Fi, replace `localhost` with your machine's local IP (e.g. `http://192.168.1.x:8080`).
